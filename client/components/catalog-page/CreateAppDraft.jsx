const Button = ReactBootstrap.Button;

CreateAppDraft = React.createClass({
  render(){
    return <div>
      Can't find the app?
      <br/>
      <Button>
        Create new app
      </Button>
    </div>
  }
});